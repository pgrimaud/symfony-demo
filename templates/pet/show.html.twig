{% extends 'base.html.twig' %}

{% block title %}Pet{% endblock %}

{% block body %}
    <h1>Pet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pet.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td><img src="/uploads/pets/{{ pet.picture }}" style="width:100px;"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pet_index') }}">back to list</a>

    <a href="{{ path('pet_edit', {'id': pet.id}) }}">edit</a>

    {{ include('pet/_delete_form.html.twig') }}
{% endblock %}
